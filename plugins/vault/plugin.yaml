manifest_version: 1
name: vault
version: 0.1.0
description: "BISEO vault tools: read, write, list, search and backlinks over the user's markdown vault."
author: BISEO
kind: standalone
provides_hooks:
  - pre_llm_call
provides_tools:
  - vault_read
  - vault_write
  - vault_list
  - vault_search
  - vault_backlinks
  - money_add_transactions
  - money_summary
